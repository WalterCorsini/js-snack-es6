/**
 * Description: creates a new array with numbers between a minimum and a maximum
 * @param {object} arr
 * @param {number} min
 * @param {number} max
 * @returns {object}
 */
function createString(arr,min,max){
    // arrow function
    let string;
    return string = arr.filter((element,index) => index >= a && index <= b);
//  if(index >=a && index <=b){
//     string.push(element);
//  }
//  return string;
};

